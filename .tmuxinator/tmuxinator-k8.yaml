name: k8_dev

on_project_start: minikube start && ./deploy_k8.sh

windows:
  - editor:
      layout: tiled
      panes:
        - sleep 60 && ibazel -run_command_after_success='./deploy_k8.sh -r' -debounce='10s' build //store:store_image
        -
        - while true; do stern statefulset/store; done
        -
