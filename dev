#!/bin/bash

# Check for required argument
if [ "$#" -ne 1 ]; then
    echo "Usage: $0 <arg>"
    exit 1
fi

# Choose the command based on the argument
case $1 in
    dc)
        tmuxinator start -p ./.tmuxinator/tmuxinator-dc.yaml
        ;;
    k8-init)
        minikube delete && minikube start && ./deploy_k8.sh
        ;;
    k8)
        tmuxinator start -p ./.tmuxinator/tmuxinator-k8.yaml
        ;;
    k8!)
        tmux kill-session -t k8_dev
        ;;
    e2e)
        k6 run test.js
        ;;
    test)
        ibazel test --test_output=errors //store:go_default_test
        ;;
    deps)
        ./update_deps.sh
        ;;
    *)
        echo "Invalid argument."
        exit 1
        ;;
esac
